// this just does a webpack JSON require and
// then exports the resulting objects.
export const matasano = require('json!./matasano.json')
export const blogPosts = require('json!./blog.json')
export const pages = require('json!./pages.json')
